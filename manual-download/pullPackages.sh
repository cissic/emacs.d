#!/bin/bash

# git clone https://github.com/astahlman/ob-async.git
# and add:
#   (add-to-list 'load-path "$PATH_TO_OB_ASYNC_ROOT_DIR")
#   (require 'ob-sync) 
# to init.el


git clone https://codeberg.org/FelipeLema/session-async.el
# and add:
# (add-to-list 'load-path "~/some/path/session-async.el") ;; add repo directory, not the file
# (require 'session-async)
# to init.el
